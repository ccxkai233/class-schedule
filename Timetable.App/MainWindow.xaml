<Window x:Class="Timetable.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Timetable.App"
        xmlns:vm="clr-namespace:Timetable.App.ViewModels"
        mc:Ignorable="d"
        Title="电子课程表" Height="800" Width="1280"
        Background="Transparent" Foreground="White"
        WindowStyle="None" AllowsTransparency="True" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="WindowCloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C42B1C"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Border Background="#2D2D30" CornerRadius="8" BorderBrush="#444" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="Transparent" MouseDown="TitleBar_MouseDown">
                <Grid>
                    <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" 
                               VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" Opacity="0.8"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="_" Width="46" Click="Minimize_Click" Style="{StaticResource WindowControlButton}"/>
                        <Button Content="[ ]" Width="46" Click="Maximize_Click" Style="{StaticResource WindowControlButton}"/>
                        <Button Content="X" Width="46" Click="Close_Click" Style="{StaticResource WindowCloseButton}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="16,0,16,16" DataContext="{Binding TodayViewModel}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <!-- Left: Today's Schedule -->
                <ListBox Grid.Column="0" ItemsSource="{Binding TodaySlots}" SelectedItem="{Binding CurrentSlot}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         SelectionMode="Single" Background="Transparent" BorderThickness="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" Padding="12" Margin="0,8" Background="#3E3E42">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                <Setter Property="Background" Value="#007ACC"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="{Binding TimeRange}" FontSize="14" Opacity="0.7" Foreground="White"/>
                                    <TextBlock Text="{Binding SubjectName}" FontSize="18" FontWeight="SemiBold" Margin="0,4,0,8" Foreground="White"/>
                                    <ProgressBar Value="{Binding ProgressPercent}" Height="4"
                                                 Foreground="LightGreen" Background="Transparent"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- Right: Current Info Panel -->
                <StackPanel Grid.Column="1" Margin="24,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="{Binding NowDateText}" FontSize="20" Opacity="0.7"/>
                    <TextBlock Text="{Binding NowTimeText}" FontSize="48" FontWeight="Bold" Margin="0,8,0,0"/>
                    <TextBlock Text="{Binding CurrentSubjectName}" FontSize="28" Margin="0,16,0,0"/>
                    <TextBlock Text="{Binding CountdownText}" FontSize="24" Opacity="0.8" Margin="0,4,0,0"/>
                    <Separator Margin="0,24"/>
                    <TextBlock Text="{Binding NextSubjectText}" FontSize="18" Opacity="0.9"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
